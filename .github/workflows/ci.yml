name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: lint + types + tests (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      # CPU wheels only. pyproject deliberately does not pin torch to a CUDA
      # index, so this stays a ~200 MB install instead of multi-GB.
      - name: Install torch (CPU)
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

      - name: Install package
        run: pip install -e ".[dev]"

      - name: Ruff (lint)
        run: ruff check .

      - name: Mypy
        run: mypy

      # The whole suite runs on synthesised raw interferograms + a synthetic
      # .CLB (see tests/conftest.py), so no instrument data is required.
      - name: Pytest
        run: pytest -q

      - name: Syntax check
        run: python -m compileall -q src tests
